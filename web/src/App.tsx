// Componente radice di TaskFlow.
// TODO (con l'AI): genera e monta TaskList, TaskForm, TaskCard, StatusFilter.
export function App() {
  return (
    <main>
      <h1>TaskFlow</h1>
      <p>
        Scheletro pronto. Costruisci i componenti con l'AI (vedi Lab Guide M6).
      </p>
    </main>
  );
}
